// Import All SVGs into one file

import { ReactComponent as closedEye } from "../SVGs/closedEye.svg";
import { ReactComponent as eye } from "../SVGs/eye.svg";
import { ReactComponent as logout } from "../SVGs/logout.svg";
import { ReactComponent as settings } from "../SVGs/settings.svg";
import { ReactComponent as burger } from "../SVGs/burger.svg";

// Assign for easier single imports

export const UnifiedSVGSize = 24;
export const ClosedEye = closedEye;
export const Eye = eye;
export const LogoutIcon = logout;
export const Settings = settings;
export const Burger = burger;

//TODO Setup Global Basic SVG Icons' styles
